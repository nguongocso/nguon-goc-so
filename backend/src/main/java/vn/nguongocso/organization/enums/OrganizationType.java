package vn.nguongocso.organization.enums;

/** Loại tổ chức. */
public enum OrganizationType {
    /** Hợp tác xã (VT02, VT03). */
    COOPERATIVE,

    /** Doanh nghiệp (VT04). */
    ENTERPRISE,

    /** Cán bộ ngành (VT05). */
    GOVERNMENT,

    /** Tổ chức hệ thống (Admin - VT01). */
    SYSTEM
}
