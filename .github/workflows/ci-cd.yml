name: CI/CD

on:
  push:
    branches:
      - develop
      - main
      - test/cicd
  pull_request:
    branches:
      - develop
      - main
      - test/cicd
  workflow_dispatch:
    inputs:
      environment:
        description: "Deployment environment"
        required: true
        type: choice
        default: staging
        options:
          - staging
          - production

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ${{ github.repository_owner }}/nguongocso-backend
  FRONTEND_IMAGE: ${{ github.repository_owner }}/nguongocso-frontend

jobs:
  # ==========================================================
  # BACKEND - TEST
  # ==========================================================
  backend-test:
    name: Backend - Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Make Maven wrapper executable
        run: chmod +x mvnw

      # Tests use the H2 in-memory profile (application-test.properties).
      - name: Run backend tests
        env:
          SPRING_PROFILES_ACTIVE: test
        run: ./mvnw clean test

  # ==========================================================
  # FRONTEND - LINT + BUILD
  # ==========================================================
  frontend-build:
    name: Frontend - Lint + Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      # Runtime-config approach: no Vite env vars needed for production build.
      - name: Build frontend
        run: npm run build

  # ==========================================================
  # BUILD + PUSH IMAGES
  # ==========================================================
  build-push:
    name: Build & Push Images
    needs: [backend-test, frontend-build]
    if: github.event_name == 'push' && (contains(github.ref, 'develop') || contains(github.ref, 'main') || contains(github.ref, 'test/cicd'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Determine image tag
        id: tag
        run: |
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          else
            echo "tag=edge" >> "$GITHUB_OUTPUT"
          fi

      - name: Build & push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE }}:${{ steps.tag.outputs.tag }}
            ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend

      - name: Build & push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE }}:${{ steps.tag.outputs.tag }}
            ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,mode=max,scope=frontend

  # ==========================================================
  # DEPLOY TO KUBERNETES
  # ==========================================================
  deploy:
    name: Deploy to Kubernetes
    needs: build-push
    if: github.event_name == 'push' && (contains(github.ref, 'develop') || contains(github.ref, 'main') || contains(github.ref, 'test/cicd'))
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Configure kubectl
        uses: azure/setup-kubectl@v4

      - name: Set up Kubeconfig from secrets
        env:
          KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}
        run: |
          mkdir -p "$HOME/.kube"
          echo "${KUBECONFIG_B64}" | base64 -d > "$HOME/.kube/config"
          chmod 600 "$HOME/.kube/config"

      - name: Apply Kubernetes manifests
        env:
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
        run: |
          NS="${KUBE_NAMESPACE:-nguongocso}"
          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/secrets.yaml -n "$NS"
          kubectl apply -f k8s/configmap.yaml -n "$NS"
          kubectl apply -f k8s/persistent-volumes.yaml -n "$NS"
          kubectl apply -f k8s/backend-service.yaml -n "$NS"
          kubectl apply -f k8s/backend-deployment.yaml -n "$NS"
          kubectl apply -f k8s/frontend-service.yaml -n "$NS"
          kubectl apply -f k8s/frontend-deployment.yaml -n "$NS"
          kubectl apply -f k8s/ingress.yaml -n "$NS"

      - name: Apply MySQL (if using in-cluster)
        run: |
          if [ -f k8s/mysql.yaml ]; then
            kubectl apply -f k8s/mysql.yaml -n "$NS"
          fi

      - name: Restart deployments
        run: |
          kubectl rollout restart deployment/backend -n "$NS"
          kubectl rollout restart deployment/frontend -n "$NS"

      - name: Verify rollout
        env:
          KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
        run: |
          NS="${KUBE_NAMESPACE:-nguongocso}"
          kubectl -n "$NS" rollout status deployment/backend --timeout=180s
          kubectl -n "$NS" rollout status deployment/frontend --timeout=180s